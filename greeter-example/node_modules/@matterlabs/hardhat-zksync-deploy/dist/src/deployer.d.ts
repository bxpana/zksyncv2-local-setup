import { HardhatRuntimeEnvironment } from 'hardhat/types';
import * as zk from 'zksync-web3';
import * as ethers from 'ethers';
import { ZkSyncArtifact } from './types';
export declare type Overrides = ethers.Overrides & {
    feeToken?: string;
};
/**
 * An entity capable of deploying contracts to the zkSync network.
 */
export declare class Deployer {
    hre: HardhatRuntimeEnvironment;
    ethWallet: ethers.Wallet;
    zkWallet: zk.Wallet;
    constructor(hre: HardhatRuntimeEnvironment, zkWallet: zk.Wallet);
    static fromEthWallet(hre: HardhatRuntimeEnvironment, ethWallet: ethers.Wallet): Deployer;
    /**
     * Loads an artifact and verifies that it was compiled by `zksolc`.
     *
     * @param contractNameOrFullyQualifiedName The name of the contract.
     *   It can be a contract bare contract name (e.g. "Token") if it's
     *   unique in your project, or a fully qualified contract name
     *   (e.g. "contract/token.sol:Token") otherwise.
     *
     * @throws Throws an error if a non-unique contract name is used,
     *   indicating which fully qualified names can be used instead.
     *
     * @throws Throws an error if an artifact was not compiled by `zksolc`.
     */
    loadArtifact(contractNameOrFullyQualifiedName: string): Promise<ZkSyncArtifact>;
    /**
     * Estimates the price of calling a deploy transaction in a certain fee token.
     *
     * @param artifact The previously loaded artifact object.
     * @param constructorArguments List of arguments to be passed to the contract constructor.
     * @param feeToken Address of the token to pay fees in. If not provided, defaults to ETH.
     *
     * @returns Calculated fee in wei of the corresponding fee token.
     */
    estimateDeployFee(artifact: ZkSyncArtifact, constructorArguments: any[], feeToken?: string): Promise<ethers.BigNumber>;
    /**
     * Sends a deploy transaction to the zkSync network.
     * For now, it will use defaults for the transaction parameters:
     * - fee amount is requested automatically from the zkSync server.
     *
     * @param artifact The previously loaded artifact object.
     * @param constructorArguments List of arguments to be passed to the contract constructor.
     * @param overrides Optional object with additional deploy transaction parameters.
     *
     * @returns A contract object.
     */
    deploy(artifact: ZkSyncArtifact, constructorArguments: any[], overrides?: Overrides): Promise<zk.Contract>;
    /**
     * Extracts factory dependencies from the artifact.
     *
     * @param artifact Artifact to extract dependencies from
     *
     * @returns Factory dependencies in the format expected by SDK.
     */
    extractFactoryDeps(artifact: ZkSyncArtifact): Promise<string[]>;
}
//# sourceMappingURL=deployer.d.ts.map